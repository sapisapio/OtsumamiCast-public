# 0.2.7 修正内容

- 最近使ったスタンプを軽量なWebSocket通知で同期し、ホスト自身の使用履歴も保存。
- リスナーによるスタンプ追加をIPごとに5分間5件へ制限。ホストの追加操作は対象外。

## 0.2.6 修正内容

まりもさんの改良を含む公開版0.2.5を基準としています。

- オーバーレイのサイズと通知バーの位置を自動保存し、再起動後に復元。モニター変更で画面外になった位置は復帰。
- アプリ上部からポートを設定し、次回起動時にサーバー、各ウィンドウ、UPnPへ反映。YP一覧は従来通り7244固定。変更時はURLまたはIP:ポートを案内。
- 配信者へ接続したアプリのサムネイル参照先を統一。更新番号付きURLはキャッシュを再利用し、再生成時だけ番号を変更。更新日時がない場合に毎回現在時刻を付ける処理を除去。
- 未初期化プレイヤーへの同期は最新の通知を保持し、ユーザーがタブを開いてプレイヤーが準備できてから適用。同期によるタブ強制移動を廃止。シークしきい値は変更なし。
- 同梱FFmpeg/ffprobeを優先使用し、起動エラー時に失敗として処理。
- 着信音と音量を保存・再読込・オーバーレイ再作成時に適用。
- プロフィール更新を保存済みの全公開項目から配信。空リンク配列・ふりがな・スキン等を維持し、受信経路を一本化。
- 管理HTTP APIはローカル制限。パスワード、BAN一覧、投稿者IPをリスナー向けデータから除去。カテゴリ変更のロール・定義を検証。
- 別ホストのおひねり設定を正しい接続先から取得。
- スタンプ送信ごとの全一覧配信を除去。遅延タブ初期化、カテゴリ選択の保持、ホバー終了後の画像表示、キュー自動送りの重複を修正。
- ウィンドウ読込再試行を共通化。破棄後の再試行を停止。起動失敗の表示と終了処理を整理。
- 公開用LICENSEを追加。実行時設定ファイルをGit管理とビルド対象から除外。

## 検証

npm test（11件成功）: 設定の保存・復元、接続URL、スタンプ操作、権限、プロフィール配信、FFmpeg起動失敗、キュー重複抑止、実HTTP/WebSocketサーバーの統合テスト。

Electron描画テスト: 別ポートへの接続、プロフィール、後から開くカテゴリUI、静止サムネイル、ポート設定保存、タブ強制移動防止、着信音と音量0の保存・再読込。サムネイル一覧の3回再描画で画像通信は初回の1回だけ。

同梱FFmpeg/ffprobeによる1秒のテスト動画の解析と、128px PNGおよび6フレームGIF生成を確認。

## 検証上の制限

この実行環境のGPU/サンドボックス制約のため、Electron描画テストに限り隔離環境向けの起動オプションを使用。製品のサンドボックス設定は弱めていません。通常のWindows環境でのOBS取り込み、実ルーターのUPnP、実YouTube配信、macOS/Linuxの動作は未確認です。
既存のFFmpegバイナリや素材は変更していません。第三者配布物のライセンス充足をこの動作確認で保証するものではありません。

Windowsディレクトリ形式のパッケージ作成も成功。検証では既存Electronを使用し、署名処理とネイティブ依存の再ビルドを省略しています。インストーラー配布の検証ではありません。
